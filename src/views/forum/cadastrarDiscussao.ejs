<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Criar - Discussão</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 min-h-screen">
  <%- include('../includes/navbar') %>

    <div class="flex items-center justify-center min-h-[calc(100vh-64px)]">
      <div class="bg-white shadow-lg rounded-lg p-6 w-full max-w-md">
        <h1 class="text-2xl font-bold text-center mb-4">Cadastrar Discussão</h1>
        <form action="/forum/cadastrarDiscussao" method="POST" enctype="multipart/form-data" class="flex flex-col gap-3 mb-4">
          <% if (error) { %>
            <p style="color: red;">
              <%= error %>
            </p>
            <% } %>
              <input type="text" name="titulo" placeholder="titulo" value="<%= formData ? formData.titulo : '' %>" required class="p-2 border rounded">
              <textarea name="texto" placeholder="Texto" required class="p-2 border rounded"></textarea>
              <label>Imagem da discussão (opcional)</label>
              <input type="file" name="forum" accept="image/*" class="p-2 border rounded">
              <input type="hidden" name="topico_id" value="<%=topico_id%>">
              <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">
                Cadastrar
              </button>
        </form>
      </div>
    </div>
</body>

</html>