<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cadastrar Instituição</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen flex flex-col">
  <%- include('../includes/navbar') %>

  <div class="flex items-center justify-center flex-1 p-4 sm:p-6">
    <div class="bg-white shadow-lg rounded-lg p-6 w-full max-w-md">
      <h1 class="text-2xl font-bold text-center mb-4">Cadastrar Instituição</h1>

      <% if (error) { %>
        <div class="bg-red-100 text-red-700 p-2 rounded mb-3">
          <%= error %>
        </div>
      <% } %>

      <% if (success) { %>
        <div class="bg-green-100 text-green-700 p-2 rounded mb-3">
          <%= success %>
        </div>
      <% } %>

      <form action="/cadastroInstituicao" method="POST" class="flex flex-col gap-3">
        <input 
          type="text" 
          name="nome" 
          placeholder="Nome da instituição" 
          required 
          class="p-2 border rounded"
          value="<%= typeof nome !== 'undefined' ? nome : '' %>"
        />

        <input 
          type="text" 
          name="estado" 
          placeholder="Estado (UF)" 
          maxlength="2"
          required 
          class="p-2 border rounded"
          value="<%= typeof estado !== 'undefined' ? estado : '' %>"
        />

        <input 
          type="text" 
          name="cidade" 
          placeholder="Cidade" 
          required 
          class="p-2 border rounded"
          value="<%= typeof cidade !== 'undefined' ? cidade : '' %>"
        />

        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 transition">
          Cadastrar
        </button>
      </form>
    </div>
  </div>
</body>
</html>
